async def find_rate_and_convert(value, currency, day, oer, rates):    try:        rate = rates[day][currency]    except KeyError:        result = await oer.convert(currency, "EUR", value, day)    else:        result = rate.convert_internal("EUR", value)    return result